function Ship(length, numHits) {
  let ship = {
    lenght: length,
    numHits: numHits,
    isSunk: false,
  };
}

function isSunk() {
  if (shipLength.length < 0 && numHits * shipLength > 0) {
    return true;
  }
}
